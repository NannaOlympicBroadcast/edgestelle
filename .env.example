# ============================================================
# EdgeStelle 配置文件模板
# 复制为 .env 后填入实际值
# ============================================================

# ---------- MQTT Broker ----------
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USE_TLS=false
# TLS 证书路径（启用 TLS 时必填）
MQTT_CA_CERT=deploy/certs/ca.crt
MQTT_CLIENT_CERT=deploy/certs/client.crt
MQTT_CLIENT_KEY=deploy/certs/client.key

# ---------- 安全 ----------
# 预共享密钥 — Agent 注册时需要携带
SECRET_KEY=change-me-to-a-strong-random-string
# AES-256 密钥（32 字节 hex），用于 Payload 加密（可选）
AES_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ---------- Master ----------
MASTER_HOST=0.0.0.0
MASTER_PORT=8000
DATABASE_URL=sqlite+aiosqlite:///./edgestelle.db

# ---------- Agent ----------
NODE_NAME=edge-node-01
